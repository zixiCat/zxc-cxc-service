get:
  summary: List
  operationId: listPets
  description: List all pets
  tags:
    - homepage
  responses:
    200:
      description: A paged array of pets
      headers:
        x-next:
          description: A link to the next page of responses
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: ../../schemas/HomePageInfo.yml
    default:
      $ref: ../../responses/UnexpectedError.yml
